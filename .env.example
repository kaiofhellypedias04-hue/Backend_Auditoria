APP_ENV=development
PORT=8000

# Banco
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auditoria_nfse
DB_SSLMODE=
DB_CONNECT_TIMEOUT=15

# Runtime paths
APP_DATA_DIR=./runtime
DATA_DIR=./runtime/saida
OUTPUT_DIR=./runtime/saida
TEMP_DIR=./runtime/temp
CERTS_DIR=./runtime/certs
CERTS_JSON_PATH=./runtime/certs.json
CREDENTIALS_JSON_PATH=./runtime/credentials.json
SECRETS_FILE_PATH=./runtime/secrets.runtime.json

# API / CORS
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# Node / Playwright
NODE_BIN=node
NPM_BIN=npm
PLAYWRIGHT_SCRIPT_PATH=./playwright_nfse_download.js
PACKAGE_JSON_PATH=./package.json
PLAYWRIGHT_TIMEOUT_MS=300000
NODE_OPTIONS=--openssl-legacy-provider

# Segredos para certificados/credenciais
# Use um dos formatos abaixo:
# 1) Por alias normalizado: CERT_PASSWORD_MEU_ALIAS=senha
# 2) JSON completo: CERT_PASSWORDS_JSON={"Meu Alias":"senha"}
# 3) Pela própria API, que salva em SECRETS_FILE_PATH
CERT_PASSWORDS_JSON={}
CREDENTIAL_PASSWORDS_JSON={}
ENABLE_KEYRING_FALLBACK=true

# Exemplo por alias
# CERT_PASSWORD_MEU_ALIAS=senha-do-certificado
# CREDENTIAL_PASSWORD_EMPRESA_X=senha-do-portal

# API Invertexto
INVERTEXTO_TOKEN=
INVERTEXTO_RPM=30
INVERTEXTO_DELAY_SECONDS=0.6

# S3 / MinIO (opcional)
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1
